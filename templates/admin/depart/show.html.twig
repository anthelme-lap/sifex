{% extends 'adminBase.html.twig' %}

{% block title %}Depart{% endblock %}

{% block body %}
    <h1>Depart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ depart.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ depart.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_depart_index') }}">back to list</a>

    <a href="{{ path('app_depart_edit', {'id': depart.id}) }}">edit</a>

    {{ include('admin/depart/_delete_form.html.twig') }}
{% endblock %}
