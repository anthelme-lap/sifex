{% extends 'adminBase.html.twig' %}

{% block title %}Arrive{% endblock %}

{% block body %}
    <h1>Arrive</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arrive.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ arrive.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_arrive_index') }}">back to list</a>

    <a href="{{ path('app_arrive_edit', {'id': arrive.id}) }}">edit</a>

    {{ include('admin/arrive/_delete_form.html.twig') }}
{% endblock %}
