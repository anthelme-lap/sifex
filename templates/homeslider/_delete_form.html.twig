<form method="post" action="{{ path('app_homeslider_delete', {'id': homeslider.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ homeslider.id) }}">
    <button class="btn">Sup</button>
</form>
